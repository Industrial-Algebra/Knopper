[package]
name = "Knopper"
version = "0.1.0"
edition = "2024"

[lib]
name = "knopper"
path = "src/lib.rs"

[features]
default = []
notcurses = ["dep:notcurses"]

[dependencies]
cliffy-core = "0.3.1"
rayon = "1.10"
notcurses = { version = "3.6.0", optional = true, features = ["vendored"] }
